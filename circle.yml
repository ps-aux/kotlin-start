#compile:
#  override:
#    - ./gradlew clean build
#
## Try to fix 'no tests' problem
#test:
#  override:
#    - /bin/true

general:
  artifacts:
    - "build/libs/unreal-scrapper-0.0.1-SNAPSHOT.jar"

notify:
  webhooks:
    - url: http://ci.absolutne.pro:3100/ci-hooks/circle-ci
